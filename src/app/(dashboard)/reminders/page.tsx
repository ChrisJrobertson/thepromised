import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function RemindersPage() {
  return (
    <PagePlaceholder
      description="Upcoming reminders, bulk dismiss actions, and custom reminder creation will be available here."
      title="Reminders"
    />
  );
}

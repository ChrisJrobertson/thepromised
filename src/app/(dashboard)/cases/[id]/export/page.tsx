import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function CaseExportPage() {
  return (
    <PagePlaceholder
      description="PDF export options and generation routes are scaffolded and ready for the next phase."
      title="Export Case File"
    />
  );
}

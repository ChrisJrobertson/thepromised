import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function NewInteractionPage() {
  return (
    <PagePlaceholder
      description="The full interaction logging form with evidence upload and reminders is next in the build sequence."
      title="Log Interaction"
    />
  );
}

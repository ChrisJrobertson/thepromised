import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function CaseLettersPage() {
  return (
    <PagePlaceholder
      description="Letter drafting, storage, and status tracking are set up in the next implementation phase."
      title="Case Letters"
    />
  );
}

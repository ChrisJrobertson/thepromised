export function EvidenceUpload() {
  return (
    <div className="rounded-md border border-dashed p-4 text-sm text-muted-foreground">
      Evidence upload component scaffolded (drag and drop + Supabase Storage integration next).
    </div>
  );
}
